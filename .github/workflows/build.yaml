name: Build Home Assistant Custom

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build (e.g., 2026.1.0). Leave empty to check for new versions.'
        required: false
        default: ''

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  check-and-build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Get upstream tags
        id: upstream
        if: inputs.version == ''
        run: |
          TAGS=$(curl -s "https://hub.docker.com/v2/repositories/homeassistant/home-assistant/tags?page_size=100" \
            | jq -r '.results[].name' \
            | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' \
            | sort -V \
            | tail -20)
          echo "tags<<EOF" >> $GITHUB_OUTPUT
          echo "$TAGS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Get existing tags
        id: existing
        if: inputs.version == ''
        run: |
          TOKEN=$(curl -s "https://ghcr.io/token?scope=repository:${{ github.repository }}:pull" | jq -r '.token')
          EXISTING=$(curl -s -H "Authorization: Bearer $TOKEN" \
            "https://ghcr.io/v2/${{ github.repository }}/tags/list" \
            | jq -r '.tags // [] | .[]' 2>/dev/null || echo "")
          echo "tags<<EOF" >> $GITHUB_OUTPUT
          echo "$EXISTING" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Find new tags
        id: new
        if: inputs.version == ''
        run: |
          NEW_TAGS=""
          while IFS= read -r tag; do
            if [ -n "$tag" ] && ! echo "${{ steps.existing.outputs.tags }}" | grep -qx "$tag"; then
              NEW_TAGS="${NEW_TAGS}${tag}"$'\n'
            fi
          done <<< "${{ steps.upstream.outputs.tags }}"
          echo "tags<<EOF" >> $GITHUB_OUTPUT
          echo "$NEW_TAGS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          if [ -n "$NEW_TAGS" ]; then
            echo "has_new=true" >> $GITHUB_OUTPUT
          else
            echo "has_new=false" >> $GITHUB_OUTPUT
          fi

      - name: Set manual version
        id: manual
        if: inputs.version != ''
        run: |
          echo "tags=${{ inputs.version }}" >> $GITHUB_OUTPUT
          echo "has_new=true" >> $GITHUB_OUTPUT

      - name: Determine tags to build
        id: build
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            echo "tags=${{ steps.manual.outputs.tags }}" >> $GITHUB_OUTPUT
            echo "should_build=true" >> $GITHUB_OUTPUT
          else
            echo "tags<<EOF" >> $GITHUB_OUTPUT
            echo "${{ steps.new.outputs.tags }}" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            echo "should_build=${{ steps.new.outputs.has_new }}" >> $GITHUB_OUTPUT
          fi

      - name: Checkout
        if: steps.build.outputs.should_build == 'true'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up Docker Buildx
        if: steps.build.outputs.should_build == 'true'
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4.0.0

      - name: Log in to GHCR
        if: steps.build.outputs.should_build == 'true'
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        if: steps.build.outputs.should_build == 'true'
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6.0.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha

      - name: Build and push
        if: steps.build.outputs.should_build == 'true'
        run: |
          TAG_FLAGS=""
          while IFS= read -r meta_tag; do
            [ -n "$meta_tag" ] && TAG_FLAGS="$TAG_FLAGS --tag $meta_tag"
          done <<< "${{ steps.meta.outputs.tags }}"

          while IFS= read -r tag; do
            [ -z "$tag" ] && continue
            echo "Building version: $tag"
            docker buildx build \
              --build-arg HA_VERSION=$tag \
              --tag ${{ env.REGISTRY }}/${{ github.repository }}:$tag \
              $TAG_FLAGS \
              --push \
              .
          done <<< "${{ steps.build.outputs.tags }}"